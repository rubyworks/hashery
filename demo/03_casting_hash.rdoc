= CastingHash

A CastingHash is a Hash that allows _casting_ procedures to
defined that the keys and values pass through upon assignment.

  c = CastingHash.new
  c.key_proc   = lambda { |x| x.to_s }
  c.value_proc = lambda { |x| x.to_s.upcase }

  c[:a] = 'a'

  c.assert == {'a'=>'A'}

